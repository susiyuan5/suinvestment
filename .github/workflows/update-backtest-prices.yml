name: Update historical backtest prices
permissions:
  contents: write
  actions: read
  pull-requests: write
concurrency:
  group: update-historical-backtest-prices
  cancel-in-progress: true
jobs:
  update-backtest-prices:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Generate, validate and publish independent data
        run: python scripts/live_data.py --task update-backtest-prices
      - name: Independent data read smoke (no Pages deployment)
        run: node scripts/live-data-smoke.mjs
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
"on":
  schedule:
    - cron: 0 12 * * 6
  workflow_dispatch: null
