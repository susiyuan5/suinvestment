# 每周定投修复与验证

日期：2026-09-09。改动位于本地工作区，未部署、未提交订单。

## 已完成的修复

1. **最终资金约束。** Core-Satellite 在所有分配与转投完成后，重新约束普通预算、Crash 余额和现金上限。按整数分分配尾差，避免多只股票四舍五入后超过上限。可显式传入佣金参数，回测已使用；页面继续由现有执行模块计算券商费用，不假设通用佣金率。
2. **阻止原因区分。** 现金不足、普通预算耗尽、数据无效和动作暂停不会生成转投资金。集中度阻止仍可在资金约束内转给 SPY。全局安全阻止归零所有资产和所有组成金额。
3. **结果一致性。** 最终计划的 Base、Extra、Crash、总额、信号金额与预算摘要对齐。Crash 使用量统计包含所有资产。手动记账仍只记录实际确认使用，未把建议自动写成成交。
4. **缺失现金语义。** `null` 表示未提供上限，与明确填写零现金区分。
5. **共用计算。** 页面和新版回测共用 `weekly-dca-engine.js`、`weekly-signal-model.js`、现有 DCA/Portfolio/CoreSatellite/MarketAnalysis 模块。周波动为个股 12 个周收益，回撤为个股 52 个周收盘；市场状态和趋势也共用计算。保留原始精度判断阈值，之后才做显示舍入。
6. **恢复状态。** 防御恢复按连续、不同周确认，同一周刷新不会增加确认次数；任一输入非 fresh 时不推进组合恢复。重新进入防御时清空旧恢复周。
7. **周预算节奏。** 按当月日历周二数量分摊普通月预算。300 在四次月份为 75、五次月份为 60。页面每次渲染重新计算，跨月不会继续沿用上月周数；回测缺失成交数据不会把漏投金额重新分摊到其他周。
8. **新收益指标。** 新版研究回测提供 XIRR、扣除外部入金影响的日度时间加权收益/最大回撤、对应 Calmar、平均现金占比与交易摩擦。没有使用“期末资产/累计入金开方”冒充定投年化收益。

Python Core-Satellite 同步最终预算和组成金额规则，JS/Python 共用新增边界案例。旧 Python L2/Core-Satellite 回测和旧仪表盘回测保留用于兼容，其旧报告及旧指标未重写，不作为当前策略验证；README 明确指向新版研究入口。

## 关键验收

- 可用现金 10、现金使用上限 30%：最终总额不超过 3，含尾差边界。
- 普通月预算耗尽、没有合格 Crash 释放：最终总额为零，不会重建 SPY 基础额。
- 零现金归零，未填现金仍允许在月预算内规划。
- Base + Extra + Crash 等于最终额；Crash 不占用普通预算。
- 每次转投后仍满足现金与月预算约束；数据/动作拦截不转投。
- 浏览器执行环境与 Node 对同一输入产生同一完整周计划。
- 截短未来数据不会改变截止日之前的成交；缺失开盘价明确报告无效，不能拿收盘价代替。
- 无涨跌、只有入金的样本收益为零；新入金不能掩盖净值回撤。

## 新回测结果

运行：`npm run backtest:weekly`。

输出：`results/weekly_dca_v1/summary.json`、`sensitivity.json`、`trades.json`、`curves.json`、`decisions.json`。记录输入、代码、配置和预设哈希，可逐周追查输入、最终计划与成交。

样本 2022-06-01 至 2026-08-14，219 个执行事件，每组累计外部入金 20,400，佣金 10 bps、滑点 5 bps。下表金额是固定币种研究单位，**未建模历史 CAD 汇率，不能解释成真实加元账户回报**。

| 场景 | 期末资产（含现金） | XIRR | 净值最大回撤 | 平均现金占比 |
|---|---:|---:|---:|---:|
| 全预算固定定投 | 42,180.54 | 36.25% | 22.49% | 3.79% |
| 同储备固定定投 | 36,751.48 | 29.04% | 18.72% | 21.93% |
| 修复后当前 v5，价格信号场景 | 28,946.65 | 16.94% | 12.83% | 45.49% |
| 新资金优先补低配（研究） | 34,791.95 | 26.22% | 16.69% | 22.63% |
| 稳定基础定投、有限加仓（研究） | 36,407.07 | 28.56% | 18.06% | 22.54% |

当前场景保留了更多现金，回撤较低，但这段样本内回报也较低。稳定基础定投与同储备固定定投更接近；它在较短起点的部分比较中占优，在最长样本中仍稍低，不能因此宣称稳定胜出。补低配也体现了回报与回撤的取舍。

另跑 2023、2024、2025 起点及佣金 100 bps/滑点 25 bps 的敏感性比较，均有效。起点预先列出、未拟合参数；区间互相重叠，因此不是独立样本外证明。取消转投的对照在这些场景中与当前策略相同，因为未触发有效转投；不能从这种相同结果判断转投有用或无用，转投分支由专项边界测试覆盖。

## 研究边界和下一步

历史价格信号使用截至前一交易日的原始收盘及按 ISO 周汇总的历史，与页面价格指标口径匹配；成交和估值用复权 OHLC 计入总回报。新闻、基本面、手动 Panic/覆盖、历史组合附加调整和券商执行限制没有可回放历史，已在输出中列为排除项。这是共用计算模块的价格信号场景，**不是完整实盘重建，也不是收益保证**。

补低配、稳定基础定投仅在研究脚本中存在，未修改默认策略阈值、资产目标权重或自动启用候选。Crash 是否累计结转、储备目标、未来基础额是否预留等资金政策仍沿用现有行为；这些会改变投入节奏，应在确定收益/回撤偏好后作为独立研究变量，而非混入本次正确性修复。

## 验证记录

- JavaScript：129 项通过（包含新端到端模块、资金、恢复、因果和指标测试）。
- Python：167 项通过（包含扩展后的共享 Core-Satellite 案例）。
- 静态合同：330 个唯一 ID、51 个 JavaScript 文件通过。
- 页面冒烟：桌面、手机、设置、健康、观察列表、数据失败路径通过。
- 新回测与起点/成本敏感性：通过；`git diff --check` 通过。
