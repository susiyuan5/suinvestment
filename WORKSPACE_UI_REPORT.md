# 界面整理验收

日期：2026-09-11

参考用户提供的 AlphaSpace 截图更新整体框架：桌面使用固定紧凑顶栏、左侧导航，以及市场状态／完整定投清单／三个独立资金池的三栏布局。六个标的明细直接显示。窄屏切换为顶部三入口和单列内容；持仓、抄底的内容及处理逻辑沿用原版。新增浏览器断言检查三栏顺序和六个明细直接可见。

首页分为本周操作、持仓、抄底三个视图；盯盘、研究与回测、数据质量移入更多工具。设置沿用原入口和分类。计算明细、成交记录、抄底执行设置及账本管理默认折叠。

导航使用 hash 和浏览器历史，旧 DOM 锚点自动进入对应视图并展开父级详情。隐藏视图不进入键盘焦点顺序。切换只更新可见性、焦点及图表尺寸，不调用策略、资金入账或成交处理。

## 验证

- Node：152 项通过；Python：167 项通过。
- JS 语法、静态 DOM 契约、简体中文检查通过。
- Pages smoke：账户设置保存、比例编辑、研究工具、股票详情、缺失数据安全处理通过。
- 独立抄底 smoke：刷新、实际买入、多标签页幂等、事务回滚、存储不可用、冲正、导出及恢复通过。
- 新导航 smoke：默认视图、旧锚点、前进后退、刷新、未提交表单保留、切换前后定投结果与账本一致通过。
- 桌面、390px 手机、200% 缩放、键盘与隐藏区域焦点、横向溢出检查通过；截图保存在忽略的 output/playwright 目录。

## 边界

策略、订单计算器、资金账本结构及持仓模型未修改，研究回测未重跑。测试使用隔离浏览器配置与模拟外部数据；不会读写个人账本或提交真实交易。当前市场快照过期时，页面仍按原规则暂停建议并显示原因。未加载的持仓和未填写现金显示未知。
