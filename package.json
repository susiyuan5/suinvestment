{
  "name": "suinvestment-dashboard",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:js": "node --check app.js && node --check watchlist.js && node --check idea-engine.js && node --check project-health.js",
    "audit:pages": "node scripts/e2e-smoke.mjs && node scripts/a11y-mobile-audit.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  },
  "engines": {
    "node": ">=24"
  }
}
